import { GuideTypo } from "../../../typography";

const Overview = () => {
  return (
    <GuideTypo.Wrapper>
      <GuideTypo.Lead>
        The Files tab is document management for the item — upload documents,
        categorize them, and tag them so they can be filtered and found later.
      </GuideTypo.Lead>
      <GuideTypo.Note>
        See the guide for how uploads, file types, and tags work together.
      </GuideTypo.Note>
    </GuideTypo.Wrapper>
  );
};

export default Overview;
